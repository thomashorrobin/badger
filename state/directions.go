package state

const (
	// North is enum for North
	North = "N"
	// South is enum for South
	South = "S"
	// East is enum for East
	East = "E"
	// West is enum for West
	West = "W"
)

const (
	// Left is enum for Left
	Left = "L"
	// Right is enum for Right
	Right = "R"
	// Forward is enum for Forward
	Forward = "F"
)
